# FIXME
#
# * formula has no effect
version: 1
title: Formula functions
properties:
  - randomUuid:
      type: string
      formula: UUIDV4()
  - timestamp:
      type: integer
      formula: NOW(true)
  - utcDateTime:
      type: string
      formula: NOW()
